# Ori and the Will of the Wisps Randomizer as AppImage

This repository is used to create an AppImage build of the Ori and the Will of the Wisps Randomizer. It includes a custom `AppRun` binary that is used to run the launcher inside the AppImage and scripts 